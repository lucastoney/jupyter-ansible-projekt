---
- name: Apt Cache aktualisieren
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Benoetigte Systempakete installieren
  ansible.builtin.apt:
    name: "{{ system_packages }}"
    state: present
